Skip the `Notify Codecov` step when CI runs on a fork. Forks lack
the upstream's Codecov token, and `send-notifications` rejects
tokenless calls; gating on `is-upstream-repository` keeps fork CI
clean -- {user}`gaborbernat`.
